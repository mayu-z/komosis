# ─────────────────────────────────────────────────────────────
# RIFT 2026 — Environment Variables
#
# Copy this file to `.env` at the repo root and fill in your keys.
# Docker Compose reads `.env` automatically.
# ─────────────────────────────────────────────────────────────

# ── Required for real LLM-powered runs ──────────────────────
# Provide at least one:
# 1) GROQ_API_KEYS (comma-separated), or
# 2) OPENAI_API_KEY fallback.
# Without an LLM key, only rule-based fixes are attempted.
GROQ_API_KEYS=gsk_key_1,gsk_key_2
GROQ_MODEL=llama-3.3-70b-versatile
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# ── GitHub token (for private repos / higher rate limits) ───
GITHUB_TOKEN=ghp_your-github-token-here

# ── PostgreSQL (defaults work with bundled container) ───────
# POSTGRES_USER=rift
# POSTGRES_PASSWORD=rift_secret
# POSTGRES_DB=rift
# POSTGRES_PORT=5432

# ── Redis (defaults work with bundled container) ────────────
# REDIS_PORT=6379

# ── Service ports ───────────────────────────────────────────
GATEWAY_PORT=3000
AGENT_PORT=8001
FRONTEND_PORT=5173

# ── Frontend (Vite) env ─────────────────────────────────────
VITE_FRONTEND_PORT=5173
VITE_PREVIEW_PORT=4173
VITE_DEV_PROXY_TARGET=http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
VITE_SOCKET_PATH=/socket.io

# ── Agent tuning ────────────────────────────────────────────
# MAX_ITERATIONS=5

# ── Gateway / Worker ────────────────────────────────────────
PORT=3000
OUTPUTS_DIR=./outputs
