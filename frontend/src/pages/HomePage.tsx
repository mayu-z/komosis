/* ──────────────────────────────────────────────────────────
 * HomePage — the initial input form page
 * ────────────────────────────────────────────────────────── */
import Layout from "@/components/Layout";
import InputSection from "@/sections/InputSection";

export default function HomePage() {
  return (
    <Layout>
      <InputSection />
    </Layout>
  );
}
