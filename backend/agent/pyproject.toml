[project]
name = "komosis-agent"
version = "0.1.0"
description = "Komosis autonomous CI/CD healing agent service"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
