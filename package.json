{
  "name": "komosis",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend/gateway",
    "backend/worker",
    "packages/contracts"
  ],
  "scripts": {
    "build": "npm run build -w @komosis/contracts && npm run build -w @komosis/gateway && npm run build -w @komosis/worker",
    "typecheck": "npm run typecheck -w @komosis/contracts && npm run typecheck -w @komosis/gateway && npm run typecheck -w @komosis/worker",
    "test:gateway": "npm run test -w @komosis/gateway",
    "test:agent": "cd backend/agent && pytest -q",
    "test": "npm run test -w @komosis/gateway",
    "test:contracts": "npm run test:contracts -w @komosis/gateway",
    "dev:gateway": "npm run dev -w @komosis/gateway",
    "dev:worker": "npm run dev -w @komosis/worker"
  },
  "engines": {
    "node": ">=20"
  }
}
