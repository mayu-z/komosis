# Shared baseline values for Railway services.
# Copy values into each Railway service's Variables tab as needed.

# ── Infrastructure ─────────────────────────────────────────
DATABASE_URL=postgresql://user:pass@host:5432/db
REDIS_URL=redis://default:password@host:6379

# ── Service discovery (private network hostnames) ─────────
AGENT_BASE_URL=http://agent:8001
GATEWAY_BASE_URL=http://gateway:3000
GATEWAY_UPSTREAM=http://gateway:3000

# ── Runtime behavior ───────────────────────────────────────
MAX_ITERATIONS=5
POLL_INTERVAL_MS=3000
MAX_POLL_ATTEMPTS=200
WORKER_CONCURRENCY=2
OUTPUTS_DIR=/app/outputs
REPOS_DIR=/tmp/repos

# ── Credentials ────────────────────────────────────────────
GITHUB_TOKEN=
GROQ_API_KEYS=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
